---
name: download-dir
description: Path to download OpenTofu/Terraform modules into. The default is `.terragrunt-cache`.
type: string
env:
  - TG_DOWNLOAD_DIR
---

When this flag is set, Terragrunt will use the specified directory to download OpenTofu/Terraform modules into.

The default is `.terragrunt-cache`.
