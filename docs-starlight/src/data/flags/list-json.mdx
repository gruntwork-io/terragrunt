---
name: json
description: |
  Output in JSON format.
type: boolean
env:
  - TG_JSON
aliases:
  - -j
---

A shorthand flag that sets `--format=json`. Outputs the list results in JSON format, which is ideal for programmatic processing and integration with other tools.
