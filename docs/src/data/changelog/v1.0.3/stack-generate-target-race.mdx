---
version: "v1.0.3"
category: "bug-fixes"
---

#### Fixed a race condition in `terragrunt stack generate`

Fixed a race condition that could cause non-deterministic file errors during `terragrunt stack generate`.
