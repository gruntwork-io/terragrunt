---
version: "v1.0.3"
category: "bug-fixes"
---

#### Fixed concurrent write conflict in `terragrunt stack generate`

Fixed non-deterministic file errors during `terragrunt stack generate` on nested stack hierarchies. These failures resulted from incorrect deduplication of symlinked paths and concurrent in-process calls racing on the shared output tree.
