---
version: "v1.0.4"
category: "bug-fixes"
---

#### Hardened module cache cleanup

Terragrunt's module cache cleanup is now strictly confined to the cache directory. Files outside the cache cannot be affected, regardless of what a downloaded module contains. The hardening applies to every supported module source.
