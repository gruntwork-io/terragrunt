---
version: "v1.0.4"
category: "bug-fixes"
---

#### Hardened module manifest handling

Terragrunt now bounds every `.terragrunt-module-manifest` cleanup entry to the manifest's own folder, so a malformed manifest cannot delete files outside the cache. Existing Terragrunt manifests keep the same gob format and are cleaned with the same path bounds.
