---
version: "v1.0.4"
category: "bug-fixes"
---

#### Stripped module-shipped manifest files from the cache

Any `.terragrunt-module-manifest` files inside a freshly downloaded module are now removed before Terragrunt's cleanup pass reads them, so a manifest shipped from the module cannot drive cache cleanup.
