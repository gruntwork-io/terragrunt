---
name: dependency-fetch-output-from-state
---

import Since from '@components/Since.astro';
import Before from '@components/Before.astro';

Support for fetching dependency outputs directly from state files.

### `dependency-fetch-output-from-state` - What it does
By default, Terragrunt retrieves dependency outputs by running `tofu output` or `terraform output` commands, which requires initializing the dependency unit and can be slow. When this experiment is enabled, Terragrunt will attempt to fetch dependency outputs directly from the remote state file, bypassing the need to initialize the dependency and significantly speeding up dependency processing.

**Current Backend Support:**

- S3 and GCS backends: Direct state reads are supported
- Azure Storage (`azurerm`) backend: Direct state reads are supported when the [`azure-backend`](/reference/experiments/active#azure-backend) experiment is also enabled
- Other backends: Falls back to the normal method (using `tofu/terraform output`)

Azure state protected with `customer_provided_key`, and Azure configurations that rely on native-only authentication, `metadata_host`, or `timeout_seconds`, also use the normal method.

GCS configurations that rely on backend-only credential environment variables, inline credentials or relative credential-file paths, service-account impersonation, custom storage endpoints, custom universe domains, or competing credential sources with different precedence also use the normal method. This preserves the native backend's authentication and endpoint behavior.

**Failure handling:**

<Before version="1.1.5">
Outside `render` and `render-json`, a network, permissions, or parsing failure from a direct state read ends the run instead of retrying through OpenTofu/Terraform. A missing state object still follows the dependency block's `mock_outputs` behavior.
</Before>

<Since version="1.1.5">
Reading state directly is an optimization. Outside `render` and `render-json`, Terragrunt retries a failed direct read with `tofu output` or `terraform output`, unless the state object is missing. If native output retrieval succeeds, only the speedup is lost. For missing outputs, configured `mock_outputs` are used when outputs are unavailable, while `skip_outputs` forces mocks. The render commands keep that same behavior.
</Since>

**OpenTofu state encryption:**

<Before version="1.1.5">
This experiment is **not compatible with OpenTofu state encryption**. When OpenTofu's [client-side state encryption](https://opentofu.org/docs/language/state/encryption/) is enabled, the encrypted envelope carries no plaintext `outputs` object. Terragrunt treats the dependency as having no outputs. Referencing a missing output can then fail with `Unsupported attribute`, while configured mock-output merging can supply a mock value instead. Disable this experiment using `--no-dependency-fetch-output-from-state` to force native output retrieval.
</Before>

<Since version="1.1.5">
The direct reader cannot decrypt state protected by OpenTofu's [client-side state encryption](https://opentofu.org/docs/language/state/encryption/) because it does not evaluate OpenTofu's encryption configuration or key providers from the `remote_state.encryption` map. Outside `render` and `render-json`, Terragrunt recognizes the encrypted envelope and retries with `tofu output` or `terraform output`, where the configured OpenTofu/Terraform binary performs decryption. If native output retrieval succeeds, only the speedup is lost. For missing outputs, configured `mock_outputs` are used when outputs are unavailable, while `skip_outputs` forces mocks. The render commands keep that same behavior. Disable the experiment when a render command must resolve real outputs from encrypted state.
</Since>

**Disabling the feature:**

You can disable the dependency-fetch-output-from-state feature using the `--no-dependency-fetch-output-from-state` flag, even when the experiment is enabled:

```bash
terragrunt run --all --experiment-mode --no-dependency-fetch-output-from-state -- plan
```

### `dependency-fetch-output-from-state` - How to provide feedback
Provide your feedback in the dedicated [GitHub discussion](https://github.com/gruntwork-io/terragrunt/discussions/5200) page. When reporting issues or providing feedback, please include:

- The backend type you're using
- Any performance improvements you've observed
- Any issues or edge cases you've encountered

### `dependency-fetch-output-from-state` - Criteria for stabilization
To transition the `dependency-fetch-output-from-state` feature to a stable release, the following must be addressed, at a minimum:

- [x] Add support for additional backends (GCS and Azure Storage)
- [x] Live direct-state-read integration coverage for S3, GCS, and Azure Storage
- [ ] Comprehensive integration testing across backend authentication, encryption, workspace, and error scenarios
- [ ] Performance benchmarking to validate speed improvements
- [x] Error handling and edge case testing
- [x] Documentation of supported backends and limitations
- [x] Handle OpenTofu state encryption gracefully (fallback or explicit error message)
- [ ] Community feedback on real-world usage
