---
name: ls-tui
status: active
since: "1.0.7"
---

Adds a `--tui` flag to `terragrunt list` that browses the discovered infrastructure estate in an interactive terminal UI.

### `ls-tui` - What it does

With the experiment enabled, `terragrunt list --tui` opens a yazi-style, three-column browser of the discovered estate: the parent directory on the left, the current directory in the middle, and a detail pane on the right.

Move the cursor with `j`/`k` (or the arrow keys), descend into a directory with `l`/`→`/`enter`, return to the parent with `h`/`←`, and quit with `q`. The highlighted entry is shown in blue, and its path is displayed along the top, abbreviated with `~` when it falls under your home directory.

The detail pane shows metadata for the highlighted entry: its kind, path, module source, and its dependencies, dependents, and the files it reads (shown relative to the unit). Plain directories show a count of the units and stacks they contain.

The TUI reuses the same discovery the rest of `list` relies on, so filters and flags such as `--no-hidden` apply. Running `terragrunt list --tui` without the experiment enabled is an error.

### `ls-tui` - How to provide feedback

TODO: Add a link to the relevant GitHub discussion or issue.

### `ls-tui` - Criteria for stabilization

To transition the `ls-tui` feature to a stable release, the following must be addressed:

- [ ] The browser renders large estates without noticeable latency.
- [ ] Actions for running commands against a highlighted unit or stack are implemented.
- [ ] The same TUI is shared with the `catalog` scaffolding experience.
- [ ] Positive feedback from users browsing real infrastructure estates.
- [ ] Integration tests covering the core browsing workflow.
