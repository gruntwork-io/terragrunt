# Terragrunt code on vendored OpenTofu

Code in this directory is written by hand for Terragrunt and builds on the OpenTofu packages in [`upstream/`](../upstream). `vend` regenerates `upstream/` and never touches this directory, so hand-written code belongs here, and `upstream/` changes only through `vend`.

